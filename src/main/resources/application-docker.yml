spring:
  config:
    activate:
      on-profile: docker
  data:
    redis:
      host: ${REDIS_HOST:redis}
      port: ${REDIS_PORT:6379}

app:
  url-shortener:
    base-url: "${APP_BASE_URL:http://localhost:${SERVER_PORT:8080}}"